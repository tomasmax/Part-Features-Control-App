import { ControlsColumnDividerPipe } from './controls-column-divider.pipe';

describe('ControlsColumnDividerPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlsColumnDividerPipe();
    expect(pipe).toBeTruthy();
  });
});
